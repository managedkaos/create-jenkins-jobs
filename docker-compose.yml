version: '3.7'
services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - 49000:8080
    container_name: jenkins-compose
    volumes:
      - jenkins:/var/jenkins_home
volumes:
   jenkins:
     name: jenkins
networks:
  default:
    name: jenkins

